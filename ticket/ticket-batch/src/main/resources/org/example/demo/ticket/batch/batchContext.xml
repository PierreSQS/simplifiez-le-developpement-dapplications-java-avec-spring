<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
        https://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context
        https://www.springframework.org/schema/context/spring-context.xsd">

	<!-- création d'un bean FileResouce en utilisant des propriétés chargées 
		depuis le fichier conf/config.properties -->
	<bean id="resourceTicket"
	      class="org.springframework.core.io.FileSystemResource">
		
		<constructor-arg type="java.lang.String" value="${file.path}"/>
	</bean>

	 <!-- création d'un bean javax.sql.DataSource en utilisant
         des propriétés chargées depuis le fichier conf/db-ticket.properties -->
     <bean id="dataSourceTicket" destroy-method="close"
		   class="org.apache.commons.dbcp2.BasicDataSource">
		
		<property name="driverClassName" value="${jdbc.driverClassName}" />
		<property name="url" value="${jdbc.url}" />
		<property name="username" value="${jdbc.username}" />
		<property name="password" value="${jdbc.password}" />
	</bean>	

	<!-- ========= chargement des fichiers *.properties ========= -->
	<context:property-placeholder location="file:${TICKET_HOME}/src/data/conf/db-ticket.properties,
										    file:${TICKET_HOME}/src/data/conf/config.properties"/>

	<!-- ========= scan des annotations ========= -->	
	<context:component-scan base-package="org.example.demo.ticket"></context:component-scan>
</beans>